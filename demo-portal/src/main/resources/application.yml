server:
  port: 8088

spring:
  application:
    name: fc-demo-portal-app
  mvc:
    log-request-details: true
  main:
    allow-circular-references: true
  security:
    oauth2:
      client:
        registration:
          demo-app-oidc:
            provider: keycloak
            client-id: federated-catalogue #demo-app
            client-secret: sAQFwN0ImAcx5LjAMJe2402TsQCpShuf
            #scope: openid
        provider:
          keycloak:
            issuer-uri: http://key-server:8080/realms/gaia-x
#            jwk-set-uri: http://key-server:8080/realms/gaia-x/protocol/openid-connect/certs

services:
  identity:
    uri:
      internal: http://localhost:8081

#keycloak:
#  auth-server-url: http://localhost:8080
#  realm: gaia-x
#  resource: federated-catalogue
#  principal-attribute: preferred_username
#  public-client: true
#  bearer-only: true

logging:
  level:
    root: INFO
    org.springframework.web: DEBUG
    org.springframework.security: DEBUG
    org.springframework.web.client.RestTemplate: DEBUG
    org.keycloak.adapters.OAuthRequestAuthenticator: DEBUG
